# NeovationLabs — Client

React + Vite + Tailwind CSS v4 frontend. See the root [README](../README.md)
for full setup instructions.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production build to dist/
npm run lint     # oxlint
```
